/**
 * Canonical site origin. No real domain has been assigned to this project yet
 * (labeled demo throughout) — set NEXT_PUBLIC_SITE_URL when one exists;
 * this placeholder keeps metadataBase/canonical/sitemap URLs well-formed
 * until then.
 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://ev-motion.example.com";
